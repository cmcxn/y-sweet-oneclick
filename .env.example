# ====== 基本必填 ======
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# 你的持久化桶名
S3_BUCKET=y-sweet-bucket
# S3 前缀（可选），用于在桶中加路径前缀
S3_PREFIX=y-docs

# 区域名（任意字符串即可满足大多数 S3 兼容实现的要求）
AWS_REGION=us-east-1

# Auth 服务对外端口（默认 3000）
AUTH_PORT=3000

# （可选）显式指定给客户端的连接串；默认使用容器内 ys://ysweet:8080
# 如果你打算从浏览器直连宿主机，请改成 ys://<你的主机IP或域名>:8080
# CONNECTION_STRING=ys://127.0.0.1:8080
